Run the following code in R command prompt before running the convert file. It may need more packages before running successfully.
Let me know if it fails to run.

`install.packages(c("tidyverse", "geojsonio"))`
